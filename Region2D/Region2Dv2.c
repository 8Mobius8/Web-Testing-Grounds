#include <cstdio>


/* Write a data structure for Region2D, as we discussed in class.
	Can define in terms of type int
	It can be a struct or a class */
struct Rect2D
{
	/* Write two methods
		Union of 2 Region2D's returns a new Region2D
		Intersection of 2 Regions2D's return a new Region2D
		Optional define as overloaded operators || and && */

	/* Write constructors or functions from creating new Regions2D s */
		
};

/* You may want to create data structures for sub-components
	E.g. points, vectors, width, height, center, etc */

/*
	Support Structs for Graphics
*/
struct Shape {
	// Array of Points
};
/* A location on a given Dimension */
struct Point {
	// Array of Dimension
	// Array of locations
};

/* Defines a scale of measurement */
struct Dimension {
	// Max
	// Min
	// inDim?
};






int main (void) {
	
	/* Various inputs: (at least the following)
		default = no inputs
		4 ints: xmin, xmax, ymin, ymax
		Two points */
	/* Handle the case of an empty Region2D */

	printf("%s\n", "Hello world!");

	return 0;
}